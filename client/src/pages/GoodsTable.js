import React from "react"; import { makeStyles } from "@material-ui/core/styles"; import { Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Paper } from "@material-ui/core"; const useStyles = makeStyles({ tableContainer: { maxWidth: 800, margin: "auto", marginTop: 32, marginBottom: 32, }, }); const goodsList = [{ id: 1, name: "Apple", price: 0.99, description: "A juicy red apple.", }, { id: 2, name: "Banana", price: 0.49, description: "A ripe yellow banana.", }, { id: 3, name: "Orange", price: 0.79, description: "A sweet orange.", },]; function GoodsTable() { const classes = useStyles(); return (<TableContainer component={Paper} className={classes.tableContainer}> <Table> <TableHead> <TableRow> <TableCell>ID</TableCell> <TableCell>Name</TableCell> <TableCell>Price</TableCell> <TableCell>Description</TableCell> </TableRow> </TableHead> <TableBody> {goodsList.map((goods) => (<TableRow key={goods.id}> <TableCell>{goods.id}</TableCell> <TableCell>{goods.name}</TableCell> <TableCell>${goods.price.toFixed(2)}</TableCell> <TableCell>{goods.description}</TableCell> </TableRow>))} </TableBody> </Table> </TableContainer>); } export default GoodsTable;